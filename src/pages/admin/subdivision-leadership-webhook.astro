---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Admin - Subdivision Leadership Webhook">
    <div class="space-y-8">
        <!-- Header -->
        <div class="flex items-center justify-between flex-wrap gap-4">
            <div>
                <h1 class="text-4xl md:text-5xl font-bold mb-2">Subdivision Leadership Webhook</h1>
                <p class="text-white/70">Manually update subdivision leadership on Discord</p>
            </div>
            <a href="/admin" class="px-4 py-2 bg-slate-700 hover:bg-slate-600 rounded-lg transition-colors">
                Back to Dashboard
            </a>
        </div>

        <!-- Status Messages -->
        <div id="statusMessage" class="hidden rounded-lg p-4 border">
        </div>

        <!-- Webhook Status Card -->
        <div class="bg-white/10 backdrop-blur-sm rounded-lg p-6 border border-white/20">
            <div class="flex items-center gap-4 mb-6">
                <div class="w-12 h-12 bg-teal-500/20 rounded-lg flex items-center justify-center">
                    <svg class="w-6 h-6 text-teal-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
                    </svg>
                </div>
                <div>
                    <h2 class="text-2xl font-bold text-teal-400">Webhook Status</h2>
                    <p class="text-white/60">Current status of the subdivision leadership Discord webhook</p>
                </div>
            </div>

            <div id="webhookStatus" class="space-y-4">
                <div class="text-center py-8 text-white/60">
                    <svg class="w-8 h-8 mx-auto mb-2 animate-spin" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
                    </svg>
                    Loading webhook status...
                </div>
            </div>
        </div>

        <!-- Information Card -->
        <div class="bg-white/10 backdrop-blur-sm rounded-lg p-6 border border-white/20">
            <div class="flex items-center gap-4 mb-6">
                <div class="w-12 h-12 bg-blue-500/20 rounded-lg flex items-center justify-center">
                    <svg class="w-6 h-6 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                </div>
                <div>
                    <h2 class="text-2xl font-bold text-blue-400">How It Works</h2>
                    <p class="text-white/60">Information about the subdivision leadership webhook</p>
                </div>
            </div>

            <div class="space-y-4 text-white/80">
                <div class="flex items-start gap-3">
                    <div class="w-6 h-6 bg-teal-500/20 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                        <span class="text-teal-400 text-sm font-bold">1</span>
                    </div>
                    <p>The webhook posts subdivision leadership to Discord <strong>manually</strong> when you click the update button.</p>
                </div>
                <div class="flex items-start gap-3">
                    <div class="w-6 h-6 bg-teal-500/20 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                        <span class="text-teal-400 text-sm font-bold">2</span>
                    </div>
                    <p>Each update <strong>deletes the previous messages</strong> and posts new ones, keeping the Discord channel clean.</p>
                </div>
                <div class="flex items-start gap-3">
                    <div class="w-6 h-6 bg-teal-500/20 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                        <span class="text-teal-400 text-sm font-bold">3</span>
                    </div>
                    <p>Data is pulled from the <strong>Department Data</strong> page (Subdivision Leadership section). The <strong>Subdivision Overseer</strong> appears at the top.</p>
                </div>
                <div class="flex items-start gap-3">
                    <div class="w-6 h-6 bg-teal-500/20 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                        <span class="text-teal-400 text-sm font-bold">4</span>
                    </div>
                    <p>Each leader shows their <strong>name</strong>, <strong>callsign</strong>, and <strong>subdivision name</strong>. Add Discord IDs for mentions to work.</p>
                </div>
            </div>
        </div>

        <!-- Discord ID Help Card -->
        <div class="bg-white/10 backdrop-blur-sm rounded-lg p-6 border border-white/20">
            <div class="flex items-center gap-4 mb-6">
                <div class="w-12 h-12 bg-purple-500/20 rounded-lg flex items-center justify-center">
                    <svg class="w-6 h-6 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                </div>
                <div>
                    <h2 class="text-2xl font-bold text-purple-400">How to Find Discord IDs</h2>
                    <p class="text-white/60">Steps to get a Discord user ID for mentions</p>
                </div>
            </div>

            <div class="space-y-4 text-white/80">
                <ol class="list-decimal list-inside space-y-2">
                    <li>Enable <strong>Developer Mode</strong> in Discord (User Settings > App Settings > Advanced > Developer Mode)</li>
                    <li>Right-click on the user you want to mention</li>
                    <li>Click <strong>"Copy User ID"</strong></li>
                    <li>Paste the ID (a long number like <code class="bg-slate-700 px-2 py-0.5 rounded text-cyan-300">123456789012345678</code>) into the Discord ID field in Department Data</li>
                </ol>
                <p class="text-sm text-white/60 mt-4">
                    Note: Discord IDs are not visible on the website - they are only used to create mentions in the Discord webhook.
                </p>
            </div>

            <div class="mt-6">
                <a href="/admin/department-data" class="inline-flex items-center gap-2 px-4 py-2 bg-purple-500 hover:bg-purple-600 rounded-lg transition-colors font-semibold">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                    </svg>
                    Edit Department Data
                </a>
            </div>
        </div>

        <!-- Force Update Section -->
        <div class="bg-white/10 backdrop-blur-sm rounded-lg p-6 border border-white/20">
            <div class="flex items-center gap-4 mb-6">
                <div class="w-12 h-12 bg-amber-500/20 rounded-lg flex items-center justify-center">
                    <svg class="w-6 h-6 text-amber-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
                    </svg>
                </div>
                <div>
                    <h2 class="text-2xl font-bold text-amber-400">Manual Update</h2>
                    <p class="text-white/60">Force an immediate update to Discord</p>
                </div>
            </div>

            <p class="text-white/70 mb-6">
                Click the button below to immediately update the subdivision leadership on Discord. This will delete the existing messages and post new ones with the latest data.
            </p>

            <button
                id="forceUpdateBtn"
                class="px-6 py-3 bg-amber-500 hover:bg-amber-600 rounded-lg transition-colors font-semibold flex items-center gap-2"
            >
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
                </svg>
                Update Now
            </button>
        </div>
    </div>

    <script type="module">
        let adminUser = null;

        async function validateSessionAndPermissions() {
            try {
                const response = await fetch('/api/auth/session', {
                    method: 'GET',
                    credentials: 'include'
                });
                const data = await response.json();

                if (!data.authenticated) {
                    window.location.href = '/admin/login';
                    return false;
                }

                adminUser = data.user;

                // Check if user has permission
                if (!hasPermission()) {
                    document.body.innerHTML = `
                        <div class="min-h-screen flex items-center justify-center bg-slate-900">
                            <div class="text-center p-8">
                                <h1 class="text-3xl font-bold text-red-400 mb-4">Access Denied</h1>
                                <p class="text-white/70 mb-4">You don't have permission to access this page.</p>
                                <a href="/admin" class="px-6 py-2 bg-primary hover:bg-primary/80 rounded-lg">Return to Dashboard</a>
                            </div>
                        </div>
                    `;
                    return false;
                }

                return true;
            } catch (error) {
                console.error('Session validation error:', error);
                window.location.href = '/admin/login';
                return false;
            }
        }

        // Check if user has permission
        function hasPermission() {
            if (!adminUser) return false;
            if (adminUser.role === 'super_admin') return true;
            if (adminUser.role === 'subdivision_overseer') return true;
            return adminUser.permissions && (
                adminUser.permissions.includes('subdivision-leadership-webhook') ||
                adminUser.permissions.includes('subdivisions')
            );
        }

        function showStatus(message, type = 'success') {
            const statusEl = document.getElementById('statusMessage');
            statusEl.textContent = message;
            statusEl.className = `rounded-lg p-4 border ${type === 'error' ? 'bg-red-500/20 border-red-500/50 text-red-200' : 'bg-green-500/20 border-green-500/50 text-green-200'}`;
            statusEl.classList.remove('hidden');
            setTimeout(() => statusEl.classList.add('hidden'), 5000);
        }

        function formatDate(timestamp) {
            if (!timestamp) return 'Never';
            const date = new Date(timestamp);
            return date.toLocaleString('en-US', {
                weekday: 'short',
                year: 'numeric',
                month: 'short',
                day: 'numeric',
                hour: '2-digit',
                minute: '2-digit',
                timeZoneName: 'short'
            });
        }

        async function loadWebhookStatus() {
            const container = document.getElementById('webhookStatus');
            try {
                const response = await fetch('/api/subdivision-leadership-webhook');
                const data = await response.json();

                if (data.success) {
                    container.innerHTML = `
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div class="bg-slate-800/50 rounded-lg p-4 border border-slate-600/50">
                                <div class="text-sm text-white/60 mb-1">Last Updated</div>
                                <div class="text-lg font-semibold text-green-400">${formatDate(data.lastSentAt)}</div>
                            </div>
                            <div class="bg-slate-800/50 rounded-lg p-4 border border-slate-600/50">
                                <div class="text-sm text-white/60 mb-1">Messages Sent</div>
                                <div class="text-lg font-semibold text-blue-400">${data.lastMessageIds?.length || 0} embeds</div>
                            </div>
                        </div>
                        <div class="mt-4 p-3 bg-blue-500/20 border border-blue-500/50 rounded-lg flex items-center gap-2">
                            <svg class="w-5 h-5 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                            </svg>
                            <span class="text-blue-200">Manual updates only - use the button below to update Discord</span>
                        </div>
                    `;
                } else {
                    container.innerHTML = `
                        <div class="p-4 bg-yellow-500/20 border border-yellow-500/50 rounded-lg">
                            <p class="text-yellow-200">No webhook data found. Use the update button below to send the subdivision leadership to Discord.</p>
                        </div>
                    `;
                }
            } catch (error) {
                console.error('Failed to load webhook status:', error);
                container.innerHTML = `
                    <div class="p-4 bg-red-500/20 border border-red-500/50 rounded-lg">
                        <p class="text-red-200">Failed to load webhook status. Please try again.</p>
                    </div>
                `;
            }
        }

        // Force update button handler
        document.getElementById('forceUpdateBtn').addEventListener('click', async () => {
            const btn = document.getElementById('forceUpdateBtn');
            const originalContent = btn.innerHTML;

            btn.disabled = true;
            btn.innerHTML = `
                <svg class="w-5 h-5 animate-spin" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
                </svg>
                Updating...
            `;

            try {
                const response = await fetch('/api/subdivision-leadership-webhook', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });

                const data = await response.json();

                if (data.success) {
                    showStatus(`Successfully updated subdivision leadership on Discord! (${data.messageCount} embeds sent)`, 'success');
                    await loadWebhookStatus();
                } else {
                    showStatus(data.message || 'Failed to update webhook', 'error');
                }
            } catch (error) {
                console.error('Force update error:', error);
                showStatus('Failed to update webhook. Please try again.', 'error');
            } finally {
                btn.disabled = false;
                btn.innerHTML = originalContent;
            }
        });

        // Initialize
        validateSessionAndPermissions().then(valid => {
            if (valid) {
                loadWebhookStatus();
            }
        });
    </script>
</Layout>
